FROM ghcr.io/open-webui/open-webui:ollama

# Expose port
EXPOSE 8080

# Set environment variables
ENV OLLAMA_HOST=0.0.0.0
ENV OLLAMA_ORIGINS=*

# The bundled image should handle startup automatically
# Just ensure the port is exposed
